# Output contract

A conversion emits `stack.yaml`, one values file per component, a human-readable README and `conversion-report.json` with warnings/TODOs. The current Stack schema is marked `v1alpha1` until aligned with the canonical Productive K3s Stack contract.
