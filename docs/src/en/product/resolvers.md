# Resolver model

Resolvers classify normalized services. PostgreSQL, Redis and MinIO currently receive dedicated community chart references. Unknown application containers fall back to Stakater Application.
