# OpenShip workflow

OpenShip remains independent. Its responsibility ends when it produces Docker/Compose artifacts. Adapters begins at that artifact boundary.

```text
Git repo → OpenShip → Compose → Adapters → PK3S Stack → catalog
```
