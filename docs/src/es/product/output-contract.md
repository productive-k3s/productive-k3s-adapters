# Contrato de salida

La conversion genera `stack.yaml`, values por componente, README y `conversion-report.json`. El esquema Stack actual se marca `v1alpha1` hasta alinearlo exactamente con el contrato canonico de Productive K3s.
